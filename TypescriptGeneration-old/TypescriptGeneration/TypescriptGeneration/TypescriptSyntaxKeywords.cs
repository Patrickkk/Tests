namespace TypescriptGeneration
{
    /// <summary>
    /// Contains tpyescript syntax keywords.
    /// </summary>
    enum TypescriptSyntaxKeywords
    {
        @interface,
        extends,
        @class,
        implements,
        module,
        @static,
        function,
        @enum
    }
}
